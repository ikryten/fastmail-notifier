{
  "name": "fastmail-notifier",
  "version": "1.0.1",
  "private": true,
  "description": "Unread-mail notifier for Fastmail. Chrome + Firefox, MV3.",
  "license": "MPL-2.0",
  "scripts": {
    "test": "node test/run.js && node test/dom.js && node test/release.js",
    "start": "web-ext run",
    "lint": "web-ext lint --self-hosted",
    "build": "web-ext build --overwrite-dest",
    "sign": "web-ext sign --channel=unlisted"
  },
  "devDependencies": {
    "jsdom": "^27.0.0",
    "web-ext": "10.6.0"
  }
}
